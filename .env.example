# Frontend
FRONTEND_PORT=3000

# Mediasoup server
SERVER_PORT=3001

# mediasoup RTC port range — must match the UDP range exposed in docker-compose
MEDIASOUP_LISTEN_IP=0.0.0.0
MEDIASOUP_ANNOUNCED_IP=127.0.0.1
MEDIASOUP_MIN_PORT=40000
MEDIASOUP_MAX_PORT=40100
